# src/graph/document/nodes/start.py

class DocumentStartNode:
    @staticmethod
    def execute(state: dict) -> dict:
        #print("📄 DocumentStartNode.execute llamado")
        #print(f"📦 State recibido en DocumentStartNode: {state}")

        return state
